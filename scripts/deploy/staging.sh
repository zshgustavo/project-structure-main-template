#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"

echo "==> Deploying to staging (placeholder)"
echo "Implement deployment via infrastructure/kubernetes/ or CI pipeline"