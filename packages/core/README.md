# Core Package

Shared domain logic, entities, and use cases.

## Usage

Import from apps and services — never duplicate business rules.

```
packages/core/
├── domain/       # Entities, value objects
├── use-cases/    # Application services
└── ports/        # Interfaces for infra adapters
```