# Worker

Background job processor.

## Responsibilities

- Async task execution (queues, scheduled jobs)
- Uses `packages/core` for shared business logic

## Local development

```bash
# Add your run command
```