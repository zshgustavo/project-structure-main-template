# Web

Frontend application.

## Responsibilities

- User interface and client-side routing
- Consumes `apps/api` and shared `packages/ui` components

## Local development

```bash
# Add your run command, e.g.:
# npm run dev
```

## Environment

Uses root `.env`. Key variables: `APP_PORT`, API base URL.